<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:id="@+id/check_card"
    android:clickable="true"
    android:focusable="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/card_padding_big">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/check_card_date"
                    style="@style/CheckCardStyle"
                    android:layout_weight="1"
                    android:text="@string/sample_check_date"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/check_card_sum"
                    style="@style/CheckCardStyle"
                    android:layout_gravity="top"
                    android:layout_weight="2"
                    android:gravity="end"
                    android:text="@string/sample_check_sum" />

            </LinearLayout>

            <TextView
                android:id="@+id/check_card_place_id"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:drawableStart="@drawable/ic_place_gray_24dp"
                android:gravity="center_vertical"
                android:text="@string/sample_check_place" />

        </LinearLayout>

        <View
            android:id="@+id/check_card_divider"
            android:background="@color/color_gray"
            android:visibility="visible"
            android:layout_width="match_parent"
            android:layout_height="1dp"/>

    </LinearLayout>

</androidx.cardview.widget.CardView>